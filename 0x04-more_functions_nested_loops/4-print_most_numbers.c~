#include "main.h"

 /**
  * _print - prints all numbers asked except some
  *
  * Return: Always (0)
  */

void _print(void)
{
	int n;

	for (n = 48 ; n <= 57 ; n++)
	{
		if (n != 50 && n != 52)
		{
			_putchar(n);
		}
	}
	_putchar('\n');
}
