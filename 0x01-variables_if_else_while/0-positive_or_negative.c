#include <stdio.sh>
#include <stdlib.h>
#include <time.h>
/**
  * main - Prints if number is positive negative or zero
  * Return: Always 0 (succes)
  */
int main(void)
{
		int n;

			srand(time(0));
				n = rand() - RAND_MAX / 2;
					/*
					 * if ( n > 0 )
					 * printf("is positive\n");
					 else ( n == 0)
					 printf("is zero\n");
					elif ( n < 0 )
				       print ("is negative\n");
				       */
					return (0);
}
