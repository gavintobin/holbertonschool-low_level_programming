#include <stdio.h>
/**
 * main - prints all single digit numbers of base ten
 * Return: Always (0) sucess
 */
int main(void)
{
	char ch;

	for (ch = 0 ; ch <= 9 ; ch++)
		putchar (ch);
	{
		putchar ('\n');
	}
	return (0);
}
