#include <stdio.h>
/**
 * main - prints all single digit numbers of base ten
 * Return: Always (0) sucess
 */
int main(void)
{
	char n;

	for (n = 0 ; n <= 9 ; n++)
		putchar (n);
	{
		putchar ('\n');
	}
	return (0);
}
