#include "main.h"

/**
 * _sqrt_recursion - returns natural square root
 * @n: int given
 * Return: -1 if no square root
 */

int _sqrt_recursion(int n)
{
	int c == 0;

	if (c == n)
	{
		return (0);
	}
	return (0);
}
